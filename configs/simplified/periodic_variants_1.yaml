# @package _global_
name: simplified/periodic_variants

hydra:
  sweeper:
    params:
      model.k_active_pos: 0.15
      model.k_quiet_pos: 0.10

defaults:
  - /simplified/periodic_variants_0
  - _self_
